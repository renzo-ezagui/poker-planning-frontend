# Vite build-time environment variables
# These are embedded into the JavaScript bundle during the Docker build process.
# To use custom values, set these variables before running `docker-compose up --build`.
VITE_API_URL=http://localhost:3000
VITE_SOCKET_URL=http://localhost:3000
